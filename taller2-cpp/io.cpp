#include <bits/stdc++.h>

using namespace std;

void fast_io() {
  ios_base::sync_with_stdio(false);
  cin.tie(NULL);
}


int main() {

  int a, a2;
  long long b;

  char c;
  string d;

  bool e;

  float f;
  long double g;

  pair <int, int> h;

  freopen("example.in", "r", stdin);
  freopen("example.out", "w", stdout);

  cin >> a >> a2 >> d;
  b = 2*a + a2;
  cout << b << " " << b << " lo escribi dos veces, " << d << endl;

  cerr << "Ahoy, error!" << endl;
  g = (long double)(5);
  cout << setprecision(10) << fixed;
  cout << g << endl;
  return 0;
}